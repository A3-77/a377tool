# 部署说明

**线上地址**：<https://a377.xyz>
**Cloudflare Pages 项目**：`a377tool`

站上有四个入口：

| 路径 | 是什么 | 需要什么 |
| --- | --- | --- |
| `/` | 门户首页 | 纯静态 |
| `/file/` | 文件工具箱 | 纯静态 |
| `/draw/` | 生图（studio / code0） | 纯静态 + `/api/code0/*` 代理 |
| `/trips/` | 周末去哪见面 | Pages Functions + D1 数据库 |

---

## 重新部署

```bash
cd web
CLOUDFLARE_API_TOKEN='<令牌>' CLOUDFLARE_ACCOUNT_ID='<账户ID>' \
  npx --yes wrangler@latest pages deploy public \
  --project-name=a377tool --branch=main --commit-dirty=true
```

注意目录是 **`public`**（不是 `.`）。`functions/` 在项目根，wrangler 会自动打包上传。

部署完**必须验一下**：

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://a377.xyz/          # 200
curl -s -o /dev/null -w "%{http_code}\n" https://a377.xyz/file/     # 200
curl -s -o /dev/null -w "%{http_code}\n" https://a377.xyz/trips/    # 200
curl -s https://a377.xyz/api/me                                     # 401 未登录
```

**接口返回 500 说明后端有问题，401 才是正常的。** 刚部署完边缘同步要几秒，
但超过 30 秒还是 500 就不是同步延迟了。

---

## 目录结构

```
web/
├── public/                  ← 静态资源（部署目录）
│   ├── index.html           门户首页
│   ├── file/index.html      文件工具箱
│   ├── assets/              共享 tokens / theme / shell / ZIP
│   ├── favicon.svg
│   ├── vendor/              pdf.js、pdf-lib、标准字体
│   ├── trips/index.html     周末去哪见面
│   ├── draw/
│   │   ├── index.html       生图落地页
│   │   ├── studio/index.html  Right Code 绘图工作台
│   │   └── code0/index.html   Code0 图片工作台
├── functions/api/
│   ├── *.js                 见面地的接口 → /api/*
│   ├── media/[[key]].js     素材上传 / 读取 / 删除 → /api/media
│   └── code0/[[path]].js    生图代理 → /api/code0/*
├── schema.sql               ← D1 建表语句
└── wrangler.toml            ← D1 + KV 绑定配置
```

---

## D1 数据库

已创建：`meet-db`，UUID `bb0be454-de0a-468f-8ac4-28da11a937a9`（写在 `wrangler.toml` 里）。

重建表结构：

```bash
npx wrangler d1 execute meet-db --remote --file=./schema.sql
```

---

## KV 命名空间（展示组件的素材存储）

后台「＋ 上传」传的图片和视频存在这里。**用 KV 不用 R2** 的原因：
R2 免费额度更大（10GB、无出口费），但**激活必须绑付款方式**；KV 免费额度是
1GB 总量 / 单值 25MiB / 每天 1000 次写，不要付款方式，够放几十张图和几秒的短视频。

创建（一次性）：

```bash
cd web && npx wrangler kv namespace create MEDIA
```

把返回的 `id` 填进 `wrangler.toml` 的 `[[kv_namespaces]]`（现在是全 0 的占位值），
然后**重新部署才生效**。

> 本地开发要显式加 `--kv=MEDIA`：`wrangler.toml` 里那条绑定只管部署，
> 本地 dev 的绑定是靠命令行标志给进去的。少了它 `env.MEDIA` 是 undefined，
> 上传会返回 503「没有绑定素材存储」。
>
> ```bash
> cd web && npx wrangler pages dev public --d1=DB --kv=MEDIA --persist-to .d1dev --port 8791
> ```

上传的接口是 `POST /api/media?key=<ADMIN_TOKEN>`，读的是 `/api/media/m/<日期>-<随机>.<扩展名>`
（公开、可长缓存 —— key 里带随机段，换素材 = 换 key）。

---

## 环境变量

已经设好这五个（用 `wrangler pages secret put`）：

| 变量 | 状态 |
| --- | --- |
| `SESSION_SECRET` | ✅ 随机 32 字节 |
| `ADMIN_TOKEN` | ✅ 管理口令 |
| `ADMIN_EMAIL` | ✅ `1357390150@qq.com` |
| `MAIL_FROM` | ✅ `onboarding@resend.dev` |
| `RESEND_KEY` | ✅ 已配，**实测发信成功** |
| `CODE0_API_KEY` | ✅ 生图模块用（`code0.ai` 的 key，51 字符） |

改任意一个：

```bash
npx wrangler pages secret put <变量名> --project-name=a377tool
```

> ⚠️ **`ADMIN_TOKEN` 护着两个后台，值得用强口令。**
> `/api/approve` 能**批准注册**（也就是发放 `/trips` 的访问权），
> `/api/site-admin` 能**改首页内容**。两个接口都没有失败次数限制。
>
> 生成一个：`openssl rand -base64 32 | tr -d '/+=' | cut -c1-32`
> 存到密码管理器。**值只写不可读**，Cloudflare 控制台和
> `wrangler pages secret list` 都只看得到名字，丢了只能重设。
>
> **当前状态（2026-09-18）：用的是弱口令，是知情后的选择。**
> 风险已说明过。要注意本仓库是公开的，这两个后台的路径写在 README 里 ——
> 路径 + 弱口令 = 任何人可进。所以**口令的值不要写进仓库**（commit message 也算，
> git 历史删不掉）。想换回强口令随时可以，命令就在上面。

> ⚠️ **改完 secret 必须重新部署才生效** —— Pages 的 secrets 是在部署时绑定的。
> 我第一次配 `CODE0_API_KEY` 后忘了重新部署，`/api/code0/status` 一直返回 `key: false`。
> 改 `ADMIN_TOKEN` 同理：控制台里改完不部署，线上还是旧口令。
>
> 验证方法：`curl -o /dev/null -w "%{http_code}" "https://a377.xyz/api/site-admin?key=<新值>"`
> 返回 200 才算生效。
>
> 不装 wrangler 也行：控制台改完值，去 **Deployments → 最新部署 → `...` → Retry deployment**。

> ⚠️ **Resend 免费版的限制**：用 `onboarding@resend.dev` 发件时，**只能发给 Resend
> 账户自己的注册邮箱**。要发给别人，得先在 Resend 后台验证 `a377.xyz` 域名
> （需要加几条 DNS 记录），然后把 `MAIL_FROM` 改成 `noreply@a377.xyz`。

---

## 管理页

两个后台**用同一个口令**（就是上面那个 `ADMIN_TOKEN`）：

| 地址 | 干什么 |
| --- | --- |
| `https://a377.xyz/api/approve?key=<你的 ADMIN_TOKEN>` | 审批注册申请 |
| `https://a377.xyz/api/site-admin?key=<你的 ADMIN_TOKEN>` | 首页展示组件（弧形画廊 / Photo Stack） |

审批页底部有「首页展示组件 →」的入口，不用记第二个地址。

### 审批注册申请

打开就能看到待审批列表，点通过 / 拒绝。

**别人注册的完整流程**：他提交申请 → 你邮箱收到审批邮件 → 点开确认页 → 再点一次通过。

> 邮件里的链接**点一下不会直接生效**，这是故意的——邮件扫描器会把 GET 链接全部预取，
> 直接生效等于垃圾注册全部自动通过。

### 首页展示组件

左侧是首页实时预览，右侧改图片和视觉参数，**点保存才写库**（不保存随便调）。

- **弧形画廊**：默认开启。`perView` / `angleStep` / `aspect` 是**比例参数**不是像素 ——
  卡片尺寸由容器宽度反解，所以换屏幕宽度不会跑偏。
- **Photo Stack**：默认关闭，右上角开关一键打开。点最上面那张换下一张（对齐 DialKit 原版）。

**怎么放自己的图片 / 视频**（三种，按方便程度排）：

1. **直接在后台传**（推荐）—— 列表右上角「＋ 上传」可以多选，也可以把文件从资源管理器
   直接拖进列表。点某一项的缩略图 = 只换那一项的素材。
   图片会**在浏览器里先压过再传**：手机直出 4032×3024 的 11.4MB 照片会变成
   1600×1200 的 612KB（实测 0.5 秒），省流量也省 KV 额度。
   Photo Stack 还会自动从图里取平均色当阴影底色，不用手填。
   传完记得点「保存」——素材是立即上传的，但**列表里的引用要保存才落库**。
2. **放进仓库再部署** —— 把文件丢进 `web/public/assets/showcase/`，
   跑一遍部署命令，URL 填 `/assets/showcase/你的文件.jpg`。
   不用配 KV，但每换一次素材都要重新部署。
3. **外链** —— URL 框里直接填任意 https 地址（图床、对象存储都行）。

传之前该按什么规格准备素材，后台面板里写着（画廊的「视频」分组顶部那块蓝色只读说明）。

改完立刻生效，前台不用重新部署（配置读的是 D1，不是构建产物）。

> ⚠️ 但**改了 `functions/` 或 `public/` 里的代码**就必须重新部署 —— 只有 D1 里的配置是热的。
> 素材上传算「配置」还是「代码」？**都不算** —— 素材存在 KV 里，上传后立刻可用，
> 不用重新部署（前提是 KV 绑定已经在线上生效过一次）。

忘了 `ADMIN_TOKEN` 怎么办：Cloudflare 的 secret 是**只写不可读**的，
控制台和 `wrangler pages secret list` 都只能看到名字，看不到值。只能重设一个：

```bash
cd web && npx wrangler pages secret put ADMIN_TOKEN --project-name=a377tool
```

**重设后必须重新部署**，而且两个后台的口令会一起变（同一个 secret）。

**这个链接里的 key 就是管理口令，别外传。**

---

## 测试账号

| 账号 | 密码 | 席位 |
| --- | --- | --- |
| 南京 | `111` | 南京方 |
| 黄石 | `222` | 黄石方 |

> **注意**：账号是**中文**的「南京」「黄石」，`111` / `222` 是**密码**，别搞反。

首次访问站点时自动种下。要改：

```bash
npx wrangler pages secret put SEED_NJ_PW
npx wrangler pages secret put SEED_HS_PW
```

改 secret 不影响已入库的账号，只对下次重新种有效——要真正改密码得直接改数据库。

---

## 排查：界面卡住 / 按钮点不动

**先看页面顶部有没有红色提示条。** 任何初始化异常都会显示在那里（不只是躺在控制台）。

这类症状几乎都是**初始化中途抛异常**导致的：事件绑定是顺序执行的，前面一行出错，
后面全部失效，而用户只看到"按钮没反应"。

已知的两个坑（都已修）：

- `Cloud.pull()` 失败会中断后续初始化 → 现在改成失败就退回本地数据
- 接口返回非 JSON 时只报 `reading 'xxx' of null` → 现在会把**具体请求和响应内容**带出来

如果红色条里显示 `拿到非 JSON 响应`，说明请求没打到后端，常见原因：

| 可能 | 特征 |
| --- | --- |
| 广告拦截扩展 | uBlock / AdGuard 会把 `/api/*` 误判成追踪请求 |
| 校园网 / 公共 WiFi 门户 | 未认证时劫持到登录页，返回 200 + HTML |
| 代理 / VPN | 中间设备替换了响应 |

排查命令：

```bash
curl -s -o /dev/null -w "%{http_code}\n" https://a377.xyz/api/me
# 401 正常（未登录）；返回 200 且是一堆 HTML 就说明被劫持了
```

---

## 主题切换

所有页面都有浅色/深色切换，共用 `assets/theme.js` 和 `localStorage` 的 **`a377theme`** 键，**切一次全站都跟着变**。

- 首次访问跟随系统 `prefers-color-scheme`
- 全局顶栏由 `assets/shell.js` 注入，主题按钮在顶栏右侧
- **见面地的地图会跟随浅色/深色主题切换**——颜色集中在 `--map-*` 变量里，高德地图会切换 light / darkblue


改配色时注意：见面地地图颜色现在集中在 `--map-*` 变量（`:root` 和 `html[data-theme="light"]` 两段），改地图时优先改这些变量。UI 层主题仍集中在 `<style>` 末尾的「浅色主题」两段。

---

## 坑（都踩过）

### 1. 别用 curl 直接调 Pages 的上传 API

Pages 的 Direct Upload 是**两段式**的：文件内容要先 `POST /pages/assets/upload`
（JSON + base64，key 是文件哈希），最后才创建 deployment 引用这些哈希。

只在创建 deployment 的请求里带 `file` 字段，会返回 `success: true`、状态也是 success，
**但访问站点是 500**。用 wrangler 就不用管这些。

### 2. PBKDF2 迭代数上限 100000

Cloudflare Workers 的 Web Crypto 对 PBKDF2 迭代数有**硬上限**，超过会抛：

```
Pbkdf2 failed: iteration counts above 100000 are not supported
```

**本地 `wrangler dev` 不校验这个**，只有部署到线上才暴露。原代码用了 150000，已改成 100000。

顺带修了一处：`verifyPassword` 原来用**当前**的 ITER 重新计算，改成用哈希串里存的迭代数——
这样以后再调 ITER 不会把老密码全废掉。

### 3. `/index.html` 返回 308 是正常的

那是 Pages 把 `/index.html` 重定向到 `/`，不是故障。

### 4. 令牌能过 `/accounts` 但过不了 `/user/tokens/verify`

账户级令牌的正常现象，不是令牌无效。

---

## 本地预览

```bash
cd web
npx wrangler pages dev public --d1=DB --persist-to .d1dev --port 8789
```

必须走 wrangler，**不能用 `python -m http.server`**——因为要跑 Functions 和 D1。
